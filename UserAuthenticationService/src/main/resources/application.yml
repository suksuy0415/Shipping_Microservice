server:
  port: 8085
spring:
  datasource:
    url: jdbc:mysql://mysqlservice:3307/registered_users?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    #url: jdbc:mysql://localhost:3306/registered_users?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect

  application:
    name: user-authentication-service


